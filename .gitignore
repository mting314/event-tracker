# build output (regenerated by build_site.py / CI)
site/dist/
# derived artifact compiled from events/*.yaml (the source of truth); rebuilt at
# deploy, fetched-by-URL by the bot, compiled-on-demand in dev. Not committed.
data/events.json

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# bot runtime
bot/*.db
bot/*.sqlite3
.env
